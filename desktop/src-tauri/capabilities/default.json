{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for all app windows",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}
